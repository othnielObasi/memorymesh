[project]
name = "memorymesh"
version = "0.1.0"
description = "MemoryMesh SDK for durable runtime memory, checkpoints, recovery, and adapters for agent frameworks."
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["memorymesh*"]
